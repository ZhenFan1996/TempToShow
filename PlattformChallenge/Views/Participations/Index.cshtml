@model IEnumerable<PlattformChallenge.Models.Participation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Challenge)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Programmer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Solution)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Challenge.C_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Programmer.User_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Solution.URL)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { item.C_Id,item.P_Id}) |
                @Html.ActionLink("Details", "Details", new { item.C_Id,item.P_Id}) |
                @Html.ActionLink("Delete", "Delete", new { item.C_Id,item.P_Id })
            </td>
        </tr>
}
    </tbody>
</table>
